name: build

on:
  push:
  pull_request:

jobs:
  xgo:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goarch:
          [windows/amd64, linux/amd64, linux/arm64, darwin/arm64, darwin/amd64]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        uses: crazy-max/ghaction-xgo@v2
        with:
          xgo_version: latest
          go_version: 1.19
          dest: build
          targets: ${{ matrix.goarch }}
          v: true
          x: false
          race: false
          ldflags: -s -w
          buildmode: default
          trimpath: true
          pkg: cmd/sequence_db
      - uses: actions/upload-artifact@v3
        with:
          name: artifact
          path: build
